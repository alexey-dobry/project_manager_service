# nginx (см. nginx.conf) проксирует /api/* → http://api-gateway:8080/*
# Поэтому фронт обращается к /api/auth/register, а бэк получает /auth/register.
VITE_API_URL=/api
VITE_API_PREFIX=
VITE_APP_ENV=production
VITE_API_TIMEOUT=15000
