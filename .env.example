# Корневой .env для docker-compose. Скопируйте: cp .env.example .env
# Содержит переменные уровня compose; локальные настройки каждого
# сервиса — в его собственном .env.example.

# Общий секрет для подписи и верификации JWT во всех сервисах.
JWT_SECRET=change-me-please-32-chars-minimum-secret
