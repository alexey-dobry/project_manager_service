// Package docs — плейсхолдер. После запуска `make swag` (`swag init -g cmd/main.go -o docs`)
// этот файл будет ПЕРЕЗАПИСАН сгенерированным `docs.go`.
//
// До генерации он нужен только для того, чтобы blank-импорт в cmd/main.go компилировался.
package docs
