# Конфиг air для hot-reload в dev-режиме
# Установка: go install github.com/air-verse/air@latest

root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/auth-service ./cmd"
  bin = "./tmp/auth-service"
  delay = 500
  exclude_dir = ["bin", "tmp", "tests", "docs", "migrations"]
  include_ext = ["go"]
  kill_delay = "1s"
  send_interrupt = true

[log]
  time = true

[color]
  main = "magenta"
  watcher = "cyan"
  build = "yellow"
  runner = "green"
